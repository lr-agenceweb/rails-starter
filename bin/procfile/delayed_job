#!/bin/sh
if [[ "$RACK_ENV" == "development" ]]; then
  if [[ "$START_DELAYED_JOB" == "true" ]]; then
    bundle exec rake jobs:work
    echo "====== DelayedJob started ======"
  else
    echo "====== DelayedJob not started ======"
  fi
fi
