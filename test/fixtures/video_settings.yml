# == Schema Information
#
# Table name: video_settings
#
#  id               :integer          not null, primary key
#  video_platform   :boolean          default(TRUE)
#  video_upload     :boolean          default(TRUE)
#  video_background :boolean          default(FALSE)
#  created_at       :datetime         not null
#  updated_at       :datetime         not null
#

one:
  video_platform: true
  video_upload: true
  video_background: true

# Test when video_upload is enabled but not video_backgrond
two:
  video_platform: true
  video_upload: true
  video_background: false

# Test when video_upload is disabled but not video_background
three:
  video_platform: true
  video_upload: false
  video_background: true