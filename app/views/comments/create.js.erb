<% unless @setting.should_validate %>
  $('.comments').prepend("<%= j render partial: 'comments/comment', locals: { comment: comment } %>");
<% end %>

$('#comment_username').val('');
$('#comment_email').val('');
$('#comment_comment').val('');

NProgress.done();

<%= render 'elements/alert' %>
