$('.comment-not-signalled').removeClass().addClass('comment-signalled').html(I18n.t('comment.signalled.already_signalled', { locale: gon.language }))

NProgress.done();
<%= render 'elements/alert' %>
<%= render 'elements/scroll_to' %>