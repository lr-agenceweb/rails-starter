li
  h3
    = link_to highlight(post.title, params[:query]), post.decorate.show_page_link
  - text = excerpt(post.content, params[:query], radius: 200)
  - highlight = text.blank? ? truncate(post.content.html_safe, length: 200, separator: '.') : highlight(text, params[:query])
  div = highlight
