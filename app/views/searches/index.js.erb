if($('#term').data('controller') == 'searches') {
  $('#search_container').html("<%= j render 'search' %>");
  NProgress.done();
}
else if(<%= params[:term].present? %>) {
  Turbolinks.visit("<%= searches_path(term: params[:term]) %>");
}