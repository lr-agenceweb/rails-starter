<%= render 'elements/scroll_infinite', object: @events, selector: '#events-container .block-grid', wrap_with: '<div class="column masonry-item" />'.html_safe %>

// Reload Masonry position for new added items
reload_masonry();
