// Reset inputs fields
inputs = ['#contact_form_name', '#contact_form_email', '#contact_form_message'];
reset_form(inputs);

// Add success state to submit button
submit_btn_state('new_contact_form', "<%= flash[:success].html_safe %>");
