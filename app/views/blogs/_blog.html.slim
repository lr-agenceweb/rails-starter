.row
  .small-12.columns
    - if @blogs.blank?
      p = t('blog.no_results')
    - else
      section#blogs itemscope='' itemtype='http://schema.org/Blog'
        - @blogs.each do |blog|
          = render 'row', blog: blog

      .row
        = paginate @blogs