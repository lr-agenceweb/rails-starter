<!DOCTYPE html PUBLIC "-//W3C//DTD XHTML 1.0 Strict//EN" "http://www.w3.org/TR/xhtml1/DTD/xhtml1-strict.dtd">
<html xmlns="http://www.w3.org/1999/xhtml">
  <head>
    <meta http-equiv="Content-Type" content="text/html; charset=utf-8" />
    <meta name="viewport" content="width=device-width" />
    <%= stylesheet_link_tag 'newsletter', media: 'all', 'data-turbolinks-track': false %>
  </head>

  <body class="order">
    <table class="body" data-made-with-foundation="">
      <tr>
        <td class="center" align="center" valign="top">
          <center>

            <!-- Upper Header -->
            <container>
              <spacer size="20"></spacer>
              <%= render 'newsletter_mailer/upper_header', newsletter_user: @newsletter_user, newsletter: @newsletter, is_welcome_user: @is_welcome_user %>
            </container>

            <!-- Lower Header -->
            <container>
              <%= raw(render 'mailer/header') %>
            </container>

            <!-- Content -->
            <container>
              <spacer size="20"></spacer>
              <%= yield %>
            </container>

            <!-- Footer -->
            <container>
              <%= raw(render 'mailer/footer') %>
            </container>

            <!-- Lower Footer -->
            <container>
              <spacer size="20"></spacer>
              <%= render 'newsletter_mailer/lower_footer', newsletter_user: @newsletter_user %>
            </container>

          </center>
        </td>
      </tr>
    </table>
  </body>
</html>
